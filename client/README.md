# EdTech Learning Task Manager – Client

React + Vite + Tailwind CSS frontend for the EdTech Learning Task Manager.

## Setup

1. Install dependencies:

   ```bash
   npm install
   ```

2. Run the dev server:

   ```bash
   npm run dev
   ```

3. The UI expects the backend to run on `http://localhost:5000`.
